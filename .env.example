# Copy to .env and set the real source.
SRT_URL=srt://cdn.itassist.one:8888?streamid=#!::r=BREAKING/NEWS,m=request

# Local HLS settings
HLS_SEGMENT_DURATION=6
HLS_TIMESHIFT_SECONDS=60
HLS_HTTP_PORT=8080
